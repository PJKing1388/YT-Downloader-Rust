[package]
name = "yt-downloader"
version = "0.1.0"
edition = "2024"
[dependencies]
reqwest = { version = "0.11", features = ["blocking"] }
dialoguer = "0.11"
youtube_dl = "0.9"  # Changed from youtube-dl to youtube_dl
tokio = { version = "1.0", features = ["full"] }

